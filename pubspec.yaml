name: BSafe
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^5.3.0
  contacts_service: ^0.3.10
  permission_handler: ^4.0.0
  cupertino_icons: ^0.1.2
  http: any
  google_maps_flutter: ^0.5.11
  flutter_speed_dial: ^1.2.5
  firebase_auth: ^0.15.5+3	
  cloud_firestore: ^0.13.4+2	
  provider: ^4.0.5	
  flutter_spinkit: ^4.1.2+1
  flutter_launcher_icons: "^0.7.3"
  flutter_timer: ^0.0.6
  dart_code_metrics: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_coverage: ^0.4.1

# if icon does not appear run command then reload app, not hot reload
# flutter pub run flutter_launcher_icons:main -f pubspec.yaml
flutter_icons:
  android: "launcher_icon"
  image_path: "assets/bee.png"

flutter:
  uses-material-design: true
